fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==23.0.0
pydantic==2.5.0
python-multipart==0.0.6
sqlalchemy==2.0.23
matplotlib==3.10.8
mpi4py==4.1.1
swig==4.4.0
pytest==7.4.3
httpx==0.27.2
methodtools>=0.4.7
numpy==2.4.6
scipy==1.18.0
numba==0.66.0
gmsh==4.15.1
# PARDISO (Intel MKL) sparse direct solver for the FEM capacitance solves -
# multithreaded and exploits symmetry, several times faster than SciPy's
# SuperLU. Optional: the solver falls back to SuperLU if it is not installed.
pypardiso==0.4.7
# PyMFEM (mfem) is built from source in the dev container (see
# .devcontainer/Dockerfile) - it is not installable from PyPI with the
# parallel/GSLIB options this project uses.

# Local Configuration Overrides
# This file is for local development overrides and should NOT be committed to git
# Copy config.local.yaml.example to config.local.yaml and customize as needed

# Example local overrides:
api:
  port: 9000  # Use a different port locally
  
database:
  url: "postgresql://localhost/my_local_db"  # Use your local database
  
# Add any other local-specific settings here...
